PROJECT(coroutine)

#包含目录

#SET(LIBRARYS )
ADD_DEFINITIONS(-DCORO_ASM=1)

ADD_LIBRARY(coro STATIC coro.c)
SET_TARGET_PROPERTIES(coro PROPERTIES LINKER_LANGUAGE C)
TARGET_LINK_LIBRARIES(coro ${LIBRARYS})


